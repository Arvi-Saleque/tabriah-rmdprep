# Ramadan Prep Course - MongoDB Integration

## ✅ সম্পন্ন কাজ

### 1. MongoDB সেটআপ
- ✅ Mongoose ইনস্টল করা হয়েছে
- ✅ MongoDB connection string `.env.local` ফাইলে সেভ করা হয়েছে
- ✅ Database connection utility তৈরি (`src/lib/mongodb.ts`)

### 2. ডেটা স্ট্রাকচার
- ✅ Mongoose Schema তৈরি (`src/models/ClassData.ts`)
- ✅ ১৬টি ক্লাসের ডেটা MongoDB তে সেভ হয়েছে

### 3. API Routes
- ✅ GET `/api/classes` - সব ক্লাসের ডেটা
- ✅ GET `/api/classes/[id]` - একটি ক্লাসের ডেটা
- ✅ PUT `/api/classes/[id]` - ক্লাস আপডেট করুন
- ✅ POST `/api/classes` - নতুন ক্লাস যুক্ত করুন
- ✅ DELETE `/api/classes/[id]` - ক্লাস মুছুন

### 4. Frontend আপডেট
- ✅ **Syllabus Page**: এখন MongoDB থেকে ডেটা লোড হয়
- ✅ **Admin Panel**: সরাসরি ডেটাবেসে সেভ হয়
- ✅ Loading states যুক্ত করা হয়েছে

### 5. Seed Script
- ✅ `npm run seed` কমান্ড দিয়ে ডেটাবেস পপুলেট করা যাবে

---

## 🚀 ব্যবহার করুন

### ডেভেলপমেন্ট সার্ভার চালু করুন
```bash
npm run dev
```

### প্রথমবার ডেটা লোড করুন (যদি ডেটাবেস খালি থাকে)
```bash
npm run seed
```

### Admin Panel ব্যবহার করুন
1. যান: `http://localhost:3000/admin`
2. পাসওয়ার্ড: `ramadan2025`
3. যেকোনো ক্লাসের "এডিট করুন" বাটনে ক্লিক করুন
4. তারিখ, ভিডিও লিংক, রিসোর্স লিংক আপডেট করুন
5. সেভ করুন - সরাসরি ডেটাবেসে সেভ হবে!

---

## 🔐 গোপনীয় তথ্য

`.env.local` ফাইলে আছে:
```
MONGODB_URI=mongodb+srv://alifsalekas_db_user:ui7f2hGrjINXHFFN@dha-cluster.pcvzzmx.mongodb.net/ramadan-prep
ADMIN_PASSWORD=ramadan2025
```

⚠️ **সতর্কতা**: এই ফাইল GitHub এ আপলোড করবেন না!

---

## 📁 নতুন ফাইল সমূহ

```
src/
├── lib/
│   └── mongodb.ts          # MongoDB connection
├── models/
│   └── ClassData.ts        # Mongoose schema
└── app/
    └── api/
        └── classes/
            ├── route.ts    # GET all, POST new
            └── [id]/
                └── route.ts # GET, PUT, DELETE single class

scripts/
└── seed.ts                 # Database seed script

.env.local                  # Environment variables (গোপনীয়!)
```

---

## 🌐 Vercel Deploy করার জন্য

1. Vercel dashboard এ যান
2. Environment Variables যুক্ত করুন:
   - `MONGODB_URI` = আপনার MongoDB connection string
   - `ADMIN_PASSWORD` = ramadan2025 (বা যেকোনো secure password)

3. Deploy করুন - কাজ করবে! ✅

---

## 🎯 এখন যা করতে পারবেন

✅ **ডেপ্লয়ের পরেও** ভিডিও/রিসোর্স লিংক আপডেট করা যাবে
✅ তারিখ পরিবর্তন করা যাবে
✅ নতুন ক্লাস যুক্ত করা যাবে (API দিয়ে)
✅ সব পরিবর্তন **স্থায়ীভাবে** সেভ হবে
✅ সব ডিভাইস থেকে আপডেটেড ডেটা দেখাবে

---

## 🔄 ভবিষ্যতে আরো ফিচার যুক্ত করা যাবে

- Student registration ডেটাবেসে সেভ করা
- Teacher profiles edit করা
- Course fee/prizes আপডেট করা
- Analytics tracking
- Email notifications

---

সব কিছু রেডি! 🎉
